[inherit]
templates_dir = /app/templates

[components]
# Disable Cognito auth for now to fix basic functionality
# cognito_auth.* = enabled

[project]
name = LearnTrac Legacy
descr = Legacy Trac system with AWS Cognito authentication
url = http://localhost:8000

[trac]
base_url = http://localhost:8000
auth_cookie_lifetime = 86400
auth_cookie_path = /

[logging]
log_type = stderr
log_level = INFO